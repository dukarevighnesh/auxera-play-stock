const MarketPulse = () => {
  return (
    <div className="fixed top-0 left-0 right-0 h-[1px] z-50 overflow-hidden bg-border/30">
      <div className="h-full w-1/3 bg-gradient-to-r from-transparent via-brand to-transparent market-pulse-line opacity-60" />
    </div>
  );
};

export default MarketPulse;
